extends layout

block content

  h1= title
  p Welcome to #{title}
   
  -for(var proj in oblist)
   a(href='#{oblist[proj].url}') #{oblist[proj].id}: #{oblist[proj].name}
   -for(var val in oblist[proj].dock)
    ul
     a(href='#{oblist[proj].dock[val].url}') #{oblist[proj].dock[val].title} 
   